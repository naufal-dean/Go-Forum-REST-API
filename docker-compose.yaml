version: '3'

services:

  onboarding-dean-database:
    image: "postgres"
    container_name: onboarding-dean-database
    volumes:
      - onboarding-dean-data:/var/lib/postgresql/data
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_DBNAME}
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5

  onboarding-dean-app:
    build: .
    container_name: onboarding-dean-app
    environment:
      - DB_HOST=onboarding-dean-database
    ports:
      - 8080:8080
    depends_on:
      onboarding-dean-database:
        condition: service_healthy
    links:
      - onboarding-dean-database

volumes:
  onboarding-dean-data: